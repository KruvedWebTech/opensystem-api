namespace OpenSystem_Api.Helpers
{
    public enum Name
    {
       
    }

    public enum StatusCodesEnum
    {
        Success = 200,
        BadRequest = 400,
        Unauthorized = 401,
        Forbidden = 403,
        NotFound = 404,
        InternalServerError = 500
    }
}